{
  "name": "flowbit-assignment",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "dev:vanna": "echo 'Run vanna with Python (see apps/vanna)'",
    "dev": "concurrently \"pnpm dev:api\" \"pnpm dev:web\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "pnpm": "^10.20.0"
  }
}
